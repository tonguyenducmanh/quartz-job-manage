namespace Job.Model
{
    /// <summary>
    /// các enum job để cấu hình cho worker biết phải chạy job nào
    /// </summary>
    public enum JobEnum
    {
        HaveANightDayJob = 1,
        ByeByeJob = 2,
        HelloJob = 3
    }
}
